<?php	$title='Retrieve - Counting Relevant Similarity';	$this->headTitle($title);?><div class="page-header">    <h1><?php echo $this->escapeHTML($title); ?> </h1></div><div class="well"> <p>The objective of this step is to derive an estimate for the new project by using the known information gathering in the <a href='<?php echo $this->url('questioner'); ?>'>answer list</a> table. Using the Nearest neighbor algorithm, SEECBR calculating how many similar project should be used. </p></div><p>	<a class="btn btn-success btn-md" href='<?php echo $this->url('questioner'); ?>'><i class="fa fa-fw fa-angle-double-left"></i>Back to My Answer List</a></p><table class="table table-bordered table-hover table-striped">	<?php for($i=0;$i<count($answer);$i++): ?>	<tr>		<td><?php echo $spesify[$i]; ?></td><td><?php echo $answer[$i]; ?></td>	</tr>	<?php endfor; ?></table><p>	<?php	$form->setAttribute('action',$this->url('fpcalculation',array('action'=>'unadjustedfunctionpoint')));	$form->prepare();		echo $this->form()->openTag($form);	echo $this->formHidden($form->get('id_answer'));	echo $this->formSubmit($form->get('submit'));	echo $this->form()->closeTag();	?></p><div class="page-header">    <h1>Similarity Percentage </h1></div><div class="well"> <p>Similarity percentage shown in% field at table below. Click <a class='btn btn-info btn-xs' href='#'><i class='fa fa-fw fa-caret-right'></i>id number</a> button if you will dapt the chosen analogies in order to generate an estimate for the new project. The tabel sorted by maximum similarity percentage to minimum except zero percent of similarity. Click <a class='btn btn-success btn-xs' href='#'>Press to Function Point Analysis</a> button if you will revise the solution.</p></div>	<table class="table table-bordered table-hover table-striped">	<tr>		<th>In %</th>		<?php 		foreach($field as $val){			echo "<th>".$val."</th>";		}		?>	</tr>	<?php 		foreach($sort as $arr):			$datArr[]=$arr;		endforeach;				for($i=0;$i<count($datasetView);$i++):			if($datArr[$i]!=0){			echo "<tr>";			echo "<td>".$datArr[$i]."</td>";$j=0;			foreach($datasetView[$i] as $val_data):				if($j==0){					echo "<td><a class='btn btn-info btn-xs' href='".$this->url('estimate',array('action'=>'adaptationprocess','id'=>$val_data))."'><i class='fa fa-fw fa-caret-right'></i>".$val_data."</a></td>";				}else{					echo "<td>".$val_data."</td>";				}				$j++;			endforeach;			echo "</tr>";			}		endfor;	?>	</table>	<?php?><p>	<?php	$form->setAttribute('action',$this->url('fpcalculation',array('action'=>'unadjustedfunctionpoint')));	$form->prepare();		echo $this->form()->openTag($form);	echo $this->formHidden($form->get('id_answer'));	echo $this->formSubmit($form->get('submit'));	echo $this->form()->closeTag();	?></p>